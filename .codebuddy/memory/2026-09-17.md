# 2026-09-17

## UI 打磨方案（用户加载了「品牌设计风格专家」技能）

**产出**：`docs/UI打磨方案.md`（现状诊断 + 4 个设计方向 + 通用打磨清单 + 3 阶段实施路径）

**做了什么**
- 新建 `tests/e2e/ui-audit.mjs`：把 `dist/renderer` 起成静态站，Chromium 注入 `window.sqlStudio` mock（假数据取用户生产库 `ads_ads_yewu.ads_ads_daily_report` 的真实列结构，400 行），一次出 8 张图（三主题主界面 + 空态 + 新建连接 + 外观面板 + 历史 + 对象浏览器），并打印逐区域背景色诊断。装了 `playwright`（`npm i --no-save`，**未写入 package.json**）+ Chromium 1243 到 `~/AppData/Local/ms-playwright/`。
- 读了 `theme.css` 全部令牌结构、`App.tsx` 主题应用、Monaco 三套主题定义、`ConnectionForm`/`ResultGrid`/`SqlEditor`。

**结论（重要，避免重复排查）**
- **主题系统逐区域全部正确**：app-shell / sidebar / top-bar / main-area / monaco-editor / result-panel / grid-header / grid-body / status-bar 在三主题下都正确变色；Monaco 实测 dark `#16171F`、light `#F5F6FA`、titanium `#F2F4F7`，与 `monaco-language.ts` 的 `editor.background` 逐一吻合 → **不需要"修主题"**。
- 中途我曾误判"浅色截图是黑的"，后用 `getComputedStyle` 逐区域验证推翻。教训：**界面颜色判断以计算样式/像素数据为准，不要只看缩略图**。
- 未覆盖：`workspace:load` 返回快照的"恢复工作区后挂载编辑器"路径（mock 返回 null）。用户 bug 1 若仍复现，需补一份带快照的 mock。

**主要发现**
- 编辑器上方三层横条（标签行 / 文件操作行 / 编辑器上下文行），中间那层右侧约 70% 空白
- 结果面板页脚与全局状态栏信息重复（耗时 / 行数），且贴到窗口底边
- 结果表头列名 + 注释都是粗体、注释用高饱和绿 → 表头抢戏
- 数字列（bigint / int）左对齐；无斑马纹、无当前行高亮
- 深色 6 层表面仅差约 10 级明度，且用实心深灰边框 → 扁平、无体积感
- 三主题品牌色相不统一（外壳蓝 `#4f7fd9` / Monaco 紫+青+绿 / 钛灰 `#4B6580`）→ 等于 4 套强调色家族
- **无 line-height / font-weight / letter-spacing 令牌**（排版层次全靠浏览器默认）
- **JetBrains Mono 未随包分发**（assets 只有 Monaco `codicon.ttf`，源码 `@font-face` 计数 0）→ 静默回退 Consolas
- 窄侧栏（约 217px）里连接表单的「保存 / 测试连接」需滚动才可见
- 外观面板用原生 `<select>`；空态无 CTA；连接表单的"取消"误用主色

**待用户决策**：选设计方向（Linear / Carbon / Raycast / Vercel）、是否统一三主题色相、是否接受合并工具栏、是否打包 JetBrains Mono、bug 1 是否仍复现。

## 修复：bit 列归一化 + Excel DECIMAL 数值化（用户指定"修 ①③"）

**改动**
- `src/main/services/query-service.ts`：`rowsToCells` 新增 `isBitColumn`（`/^bit\b/i`，`\b` 避免误匹配 `bitmap`）+ `bitBytesToNumber`（大端 ≤6 字节转 number，超出或非安全整数返回 null → 保持 Uint8Array）。**在 Main 侧归一化**，一处同时覆盖网格 / 数据预览 / Excel / CSV / SQL 导出。
- `src/main/services/excel-exporter.ts`：`toExcelValue(v, columnType)` 改为带列类型；新增 `isNumericColumnType`（容忍 `decimal(10,2)`/`bigint unsigned`）/ `toSafeExcelNumber` / `significantDigits`。仅数值型列的字符串值转真数值（整数须 `Number.isSafeInteger`；小数有效数字 ≤15），否则保持文本；`varchar`/`char` 数字串一律不转（前导零、编码不被改写）。调用点改传 `col.type`。
- 测试：`query-service.test.ts` +4（BIT(1)→0/1、BIT(16) 大端 258、56bit 保持二进制、BLOB 回归）；`excel-exporter.test.ts` +5（数值化、超精度保持文本、varchar 前导零不转、尾零 0.30→0.3、bit 0/1）。

**验证**
- `npm run typecheck` 通过；全量 `npx vitest run` = **42 文件 / 561 通过 + 1 跳过 / 0 失败**。
- 真机只读验证（腾讯云 CynosDB）：`finedb.fine_base_message` 的 `readed`/`toasted` 两列共 6 个单元格全部是 number ∈ {0,1}（此前会是 `[二进制 1 字节]`）；`ads_yewu.ads_ads_daily_report.ad_sales_amount` 仍返回精确字符串（`"0.0000"`），导出时按规则转数值。临时探针已删除。
- 文档同步：`架构文档.md`（结果集归一化 + ExcelExporter 两条）、`UI打磨方案.md`（第 14 项与 B6 标注已修复）。

**环境状态（重要）**：跑全量测试前已把 `better-sqlite3` 从 Electron ABI 恢复为 Node ABI（`Remove-Item node_modules/better-sqlite3` + `npm install` 即可，走 npm 缓存里的预编译包，**不需要 C++ 工具链**）。**再次打包 Windows 应用会被 electron-builder 改回 Electron ABI，届时跑测试前需再恢复一次。**

## UI 阶段 1 实施完成（用户决策：A+B 方向 + 接受合并工具栏）

**改动文件**：`theme.css`（三套主题令牌重写 + 网格/标签栏样式）、`EditorTabs.tsx`（两层合并为单行、文件操作改图标按钮）、`ResultGrid.tsx`（数值列右对齐）、`ResultTabs.tsx`（去掉重复"耗时"）、`monaco-language.ts`（三主题对齐品牌色与画布）、**新增 `src/shared/column-type.ts`**（`isNumericColumnType` 由 Main 导出与 Renderer 网格共用，单一来源）、**新增 `tests/shared/column-type.test.ts`**。

**最终令牌**：深色画布 `#0f1011` / 面板 `#16181a` / 次级 `#1b1d20`；边框改半透明 `rgba(255,255,255,.07/.13)`；品牌靛紫 `#5e6ad2`（hover `#7170ff`）；浅色画布 `#f7f8f8` + 品牌 `#5b62d4`；钛灰画布 `#eef0f3` + 品牌 `#565fb8`；圆角 4/6/8；新增行高 `1.29/1.45/1.6`、字重 `400/510/590`、字距 `0.16/0.32px`。

**验证**：typecheck 通过；全量 **43 文件 / 566 通过 + 1 跳过 / 0 失败**；三主题逐区域取色确认（`app-shell` = `rgb(15,16,17)`/`rgb(247,248,248)`/`rgb(238,240,243)`，`.monaco-editor` 与画布完全一致）；Monaco token 取色 `#7170FF`/`#5B62D4`/`#565FB8` 且 `squiggles=0`；前后对比截图在 `docs/ui-screenshots/audit/before|after/`。

**因行为变更更新的测试契约**：`editor-tabs.test.tsx` / `app.test.tsx`（按钮文字 → `getByTitle`；侧栏另有一个"新建"按钮必须消歧）、`result-tabs.test.tsx`（不再断言 12 ms）、`monaco-language.test.ts`（三主题色值）。

**踩坑（重要）**
- **两次因看缩略图误判颜色**：先把浅色主题看成黑的，又把靛紫关键字看成红色错误波浪线。**颜色结论一律用 `getComputedStyle` 或像素取值**；`ui-audit.mjs` 已内置逐区域背景色 + Monaco token 取色 + squiggle 计数。
- `npm install` 会把 `--no-save` 装的 `playwright` 当多余依赖清掉，跑审计脚本前需 `npm i --no-save playwright` 重装。

## 架构评审（用户要求：审视分层/设计合理性 + 文档清晰度 + 建立"改模块快速上手"的文档）

**产出**：`docs/架构评审报告.md`（230 行）与 `docs/模块改动指南.md`（201 行，新建）

**评审方式**：脚本化客观检查（跨层依赖 grep、无测试模块扫描、重复实现定位）+ 逐模块阅读。体量基线：源码 56 文件 / 11,947 行；测试 43 文件 / 8,046 行（main 16 / renderer 23 / shared 4 / e2e 7）。

**结论：架构健康，不需要重构。分层干净——**
- `renderer → @main` **0**、`renderer → node:*` **0**、`shared` 的真实引用 **0**（只有注释提到 electron）
- `main → renderer` 唯一命中是 `index.ts:116` 的 `win.loadFile(...'../renderer/index.html')`（构建产物运行时路径，非源码依赖）
- `electron` 只在 `main/index.ts`、`main/ipc.ts`、`preload/index.ts` 出现；preload 49 行纯转发
- 服务层全部构造注入 → 14 个 service 均有单测且**不连 MySQL、不需要 Electron**
- 全仓 **0 个 TODO/FIXME**（grep 到的 3 处是注释里的 `xxx` 占位符）

**真问题（已写入评审报告，建议整改顺序 1-4 可一次做完）**
1. **P1-1 标识符转义 3 处实现且同名不同义**：`sql-exporter.ts:42 escapeIdent`（**带反引号包裹**）/ `sql-utils.ts:112 escapeIdent`（**不带**）/ `schema-cache.ts:32 esc`（不带）。跨层复制会得到错层引号；且只有 `resolveTableIdent` 做了"拒绝 `;()\n\r` 片段"的校验。建议收敛到 `shared/sql-ident.ts`（`escapeIdentRaw` / `quoteIdent` / `assertSafeIdent`）。
2. **P1-2 列类型归一化 2 处且行为不一致**：`schema-cache.ts:45` 把 longblob 判成 `blob`，`query-service.ts:172` 判成 `text` → **同一列在对象浏览器与结果表头显示不同类型名**（用户可见）。另有 `query-service.ts:145 mysql2TypeToName` 需同步但无机制保证。建议收敛到 `shared/column-type.ts`（已有 `isNumericColumnType`）。
3. **P2-1 未使用依赖 `tdesign-react`**：在 `dependencies`、node_modules 已装、源码 import **0**（会进安装包）。已在开发文档技术栈删掉该行，依赖本身待移除。
4. **P2-2 巨型文件**：`theme.css` 3369（纯样式，按节定位即可，优先级最低）/ `App.tsx` 750 / `SqlEditor` 588 / `sql-completion` 556。`App.tsx` 是唯一有维护风险的（面板开关 + 业务流程混在一起，待办 #16）。
5. **P3-1** `TableFieldsPanel.tsx`(245) 是**唯一无测试组件**（`lib/`、`store/` 全部有覆盖）。
6. P3-2 `window.sqlStudio` 在渲染层有 **66 处**直接调用（可选：按域包一层 `lib/api.ts` 收敛）。

**文档缺陷（已修）**：① 开发文档 §7 小节顺序错乱（7.1→7.4→7.7→7.6→7.5）已重排为 7.1→7.7；② 开发文档写"41 个 channel"而实际 43，已改为"总数只在架构文档 §6 维护"；③ 开发文档技术栈的 `tdesign-react` 已删；④ 验证基线 42/552 → 43/566 并加日期；⑤ 架构文档 shared 目录漏 `column-type.ts`、e2e 写 6 个（实为 7 个）、App.tsx 写 700 行（实 750）已修；⑥ 两份基线文档都加了指向新文档的入口。

**新增的文档维护规则**：会漂移的数字（channel 数/行数/用例数）**只在一处维护**；新增模块必须登记到 `模块改动指南.md`；把重复实现收敛后要在该指南 §3 高危清单留一条防复发。

## 收敛整改（评审报告第 2-4 项）

**2. 标识符转义 → 新建 `src/shared/sql-ident.ts`**
- 导出 `escapeIdentRaw`（只转义）/ `quoteIdent`（转义 + 反引号包裹）/ `quoteQualifiedIdent`（解析 `db.table` 并拒绝 `;` `()` 换行片段）
- 三处调用点改为引用：`main/services/sql-exporter.ts`（删本地 `escapeIdent` 与 `resolveTableIdent`）、`main/services/schema-cache.ts`（删本地 `esc`，`SCHEMA_SQL` 改用 `quoteIdent`）、`renderer/lib/sql-utils.ts`（删本地 `escapeIdent`，`buildSelectSql` 改用 `quoteIdent`）
- 新增 `tests/shared/sql-ident.test.ts` 12 例 —— 这两个函数此前**完全没有直接测试**（只在导出流程里间接跑到）

**3. 列类型归一化 → 扩展 `src/shared/column-type.ts`**
- 新增 `columnTypeFromCode(code)`（原 query-service 的 `mysql2TypeToName`）与 `columnTypeFromRaw(raw)`（原两处 `normalizeType`）；删除两处本地实现
- **口径统一：`longblob` → `blob`**（此前 query-service 归 `text`、schema-cache 归 `blob`，同一列在对象浏览器与结果表头显示不同类型名）
- 测试 +9 例；并**显式记录既有口径**（含 `int` 子串的类型归 `int`，如 `point`/`linestring`），防止将来被"顺手修"导致行为静默变化

**4. 移除未使用依赖 `tdesign-react`**：从 `package.json` dependencies 删除，`npm install` 后 node_modules 已无该包。副作用：`npm install` 会清掉 `--no-save` 装的 playwright → 已重装（Chromium 在缓存里，无需重新下载）。

**验证**：typecheck 通过；全量 **44 文件 / 587 通过 + 1 跳过 / 0 失败**（改动前 43/566）；`npm run build` 通过（main.cjs 99.66 kB + renderer 22.43s）；改动文件 lint 0 diagnostics。
- ⚠️ 首次 build 曾报 vite `emptyDir` 失败，**重试即成功**（当时 `npm install` 正在写 node_modules，属瞬时文件占用，非代码问题）。

**文档同步**：架构文档 §4.8、开发文档 §6.2/§7.4、模块改动指南 §2.1/§2.3/§3-1、架构评审报告 §0/P1-1/P1-2 均已标记为已收敛；按既定规则在高危清单里**保留了防复发说明**。

## 交付：可运行程序 + 提交入库 + 项目规模统计

**打包**：`npm run package:dir` → `release/win-unpacked/SQL Studio.exe`（195 MB）。electron-builder 自动把 better-sqlite3 重建为 Electron ABI（日志：`install prebuilt binary better-sqlite3@12.11.1 platform=win32`）。冒烟测试通过：主进程存活，持久日志出现本轮 `app.starting` + `db.ready schema_version=2`。
- ⚠️ 打包后 `node_modules` 的 better-sqlite3 是 **Electron ABI**，再跑 `npm test` 前需 `npm install` 恢复 Node ABI。

**提交**：`daeb04f`（36 文件 +2053/-456）已推送到 `origin/master`，远端同步无差异。
- 提交前做了**凭据扫描**，发现 `tests/e2e/ui-audit.mjs` 的假连接数据用了**真实生产库主机名与账号** → 已脱敏为 `mysql-prod.internal.example.com` / `demo_reader`，并**重出了 8 张审计截图**（否则 PNG 里能看到真实主机）。
- 未纳入提交：`.codebuddy/`（工作记忆，含生产库库表名等笔记）、`docs/ui-screenshots/audit/before/`（旧 UI 对比图，图内仍有真实主机名）。

**规模统计**（排除 node_modules/dist/release，共 **132 文件 / 31,171 行**）
- 源码 **15,376**：shared 1,518 / main 3,910 / renderer 9,899（含 theme.css 3,369）/ preload 49
- 测试 **9,721**（含 e2e 1,538）、文档 **5,506**、配置 **568**
- git：**64 次提交**、累计新增 ≈48,168 行 / 删除 ≈8,554 行；**25 次提交信息里带"会话N"字样**（AI 会话推进的直接证据），12 次提到 AI 功能。

**踩坑（Windows/PowerShell）**
- PowerShell 5.1 按 ANSI 读 `.ps1` → 脚本里写中文会破坏字符串终止符；一次性脚本一律用纯 ASCII。
- 较长或含 `Remove-Item` / `foreach` 的命令容易触发审批超时 → 改用「短命令」或「写脚本文件 + `powershell -File`」。
- `git log --pretty=format:'%ad %s'` 在 Windows cmd 下引号会被拆 → 用 `--pretty=format:%ad|%s` 这类无空格格式。
