# SQL Studio 开发文档（AI 持续开发手册）

> **面向对象**：任何在本仓库开发/维护的 AI 会话。
> **用途**：让 AI 不重复踩坑、不违反架构约定，开工先看本手册再动手。
> **维护铁律**：每个任务完成后的「维护阶段任务」必须同步更新本文档（§7 坑位记录、§6 IPC 速查、§1 环境信息）。

---

## §1 环境信息（本机实测，2026-08-20）

### §1.1 Node.js

- **版本**：Node **22.22.2** / npm **10.9.7**
- **安装位置**：`C:\Users\LANGYAO\.workbuddy\binaries\node\versions\22.22.2`
- **PATH**：已加入用户 PATH（新开终端可直接用 `npm`；当前会话若报 `node 不是内部命令`，先手动注入）：
  ```powershell
  $env:Path = "C:\Users\LANGYAO\.workbuddy\binaries\node\versions\22.22.2;$env:Path"
  ```

### §1.2 网络代理与镜像（依赖安装加速）

- **系统代理**：`127.0.0.1:7897`（已启用），写死在项目根 `.npmrc`：
  ```
  proxy=http://127.0.0.1:7897
  https-proxy=http://127.0.0.1:7897
  registry=https://registry.npmmirror.com
  electron_mirror=https://npmmirror.com/mirrors/electron/
  binary_host_mirror=https://npmmirror.com/mirrors
  ```
- **安装新依赖**：直接 `npm install <pkg>` 即可，无需再配代理。
- **Electron 二进制未下载时**：手动进 `node_modules\electron` 执行 `node install.js`（会读 .npmrc 镜像）。

### §1.3 关键依赖实测版本

| 包 | 版本 | 备注 |
|---|---|---|
| electron | 37.10.3 | 高于计划"最新稳定版"要求 |
| vite | ^5.4.x | 渲染 + 主/预加载共用 |
| better-sqlite3 | ^11.3.0 | **原生模块，使用前必须 electron-rebuild** |
| mysql2 | ^3.11.x | 主进程连接池 |

---

## §2 命令速查

```bash
npm run dev          # 开发：先 build 主/preload，再并行起 Vite(5173) + Electron
npm run build        # 完整构建：typecheck → build:main → build:renderer
npm run typecheck    # tsc --noEmit
npm run test         # vitest run（全部用例）
npm run test:watch   # vitest 监听
npm run rebuild      # electron-rebuild -f -w better-sqlite3（装/升 better-sqlite3 后必跑）
npm run package      # 完整构建 + electron-builder NSIS 打包（输出 release/）
```

> 注意：任务完成校验 = `typecheck` + `test` + `build` 三连。

---

## §3 构建体系（已踩通的路径，别改）

### §3.1 三进程产物

| 进程 | 入口 | 构建方式 | 产物 | 格式 |
|---|---|---|---|---|
| 主进程 | `src/main/index.ts` | `vite build --config vite.main.config.ts` | `dist/main/main.cjs` | CJS |
| preload | `src/preload/index.ts` | 同上（双入口） | `dist/main/preload.cjs` | CJS |
| 渲染进程 | `src/renderer/main.tsx` | `vite build`（vite.config.ts） | `dist/renderer/` | ESM |

### §3.2 关键决策（改动前必读）

1. **`package.json` 声明了 `"type": "module"`**，但 Electron 主进程/预加载必须是 CommonJS
   → 主进程/preload 产物固定输出 `.cjs` 扩展名（`vite.main.config.ts` 的 `entryFileNames: '[name].cjs'`）。
   → **新增主进程入口文件时，引用 `__dirname` 等 CJS 变量的写法保持现状**。
2. **主进程构建 external 全部 npm 包 + node 内置模块**（`vite.main.config.ts` 的 external 函数）
   → 原生模块（better-sqlite3）**绝不能被打进 bundle**，否则运行时加载报错。
3. **渲染进程 dev server 端口固定 5173**，主进程通过 `VITE_DEV_SERVER_URL` 环境变量加载（`npm run dev` 里 cross-env 注入）。
4. **`src/main/index.ts` 里 preload 路径**是 `path.join(__dirname, 'preload.cjs')`（.cjs 不是 .js）。

### §3.3 vitest 双环境

- `vitest.config.ts` 默认 `environment: 'node'`（主进程服务单测）。
- **渲染进程组件测试**在测试文件第一行写注释切换：
  ```ts
  // @vitest-environment jsdom
  ```
- 路径别名：`@/*` → `src/*`，`@shared/*` → `src/shared/*`（tsconfig 与 vitest.config 均已配）。

---

## §4 代码约定（违反会被架构文档 R5/R6 追责）

### §4.1 类型唯一来源

- 跨进程类型 → 只写 `src/shared/types.ts`。
- IPC channel 常量 + 请求/响应类型 → 只写 `src/shared/ipc-contract.ts`。
- 禁止在 main/preload/renderer 内复制定义同名字段类型（如各自定义 `ConnectionSummary`）。

### §4.2 IPC 约定

- channel 一律以 `域:动作` 命名（如 `app:ping`、`connections:list`、`query:execute`）。
- 主进程 `ipcMain.handle` 的返回值统一包装：成功直接返回值；失败抛 `Error`，由路由层转 `{ ok:false, error }`。
- preload 只做 `ipcRenderer.invoke` 的类型化透传，不写业务逻辑。

### §4.3 服务层可测试性

- 每个服务构造函数**依赖注入**（连接工厂 / 存储实例 / 加密函数 / sqlite 路径），禁止内部直接 `require('electron')` 或 `new mysql2`。
- 例：`MetadataStore(path, encryptFn, decryptFn)`；`ConnectionManager(mysqlFactory)`。

### §4.4 安全红线

- 密码字段：只在主进程内存/密文落库，**任何 IPC 返回渲染进程的连接对象都不含 password**（用 `ConnectionSummary`）。
- 日志（electron-log）：只记 SQL 摘要、行数、耗时；不落密码与完整 SQL。

---

## §5 测试规范

- 测试目录：`tests/`，与 `src/` 平行（`tests/main/*`、`tests/renderer/*`、`tests/shared/*`）。
- 命名：`*.test.ts`（主进程/node 环境）或 `*.test.tsx`（组件/jsdom）。
- 主进程单测：mock 依赖注入，不连真实 MySQL。
- 组件单测：`@testing-library/react` + mock `window.sqlStudio`。
- 新任务的测试文件必须随代码同任务交付并跑通。

---

## §6 IPC 接口速查（随任务扩充，维护阶段必更新）

> 全部业务 channel 已在任务 7 完成注册（含统一 `{ok,data}` / `{ok:false,error}` 包装 + preload 解包为裸 data / reject）。

| 域 | channel | 请求 → 响应 | 实装任务 |
|---|---|---|---|
| app | `app:ping` | `() → 'pong'` | ✅ 已完成 |
| connections | `connections:list / save / remove / test / get` | 见 ipc-contract | ✅ 任务 7 |
| schema | `schema:databases / tables / columns / ddl / dataPreview` | 见 ipc-contract | ✅ 任务 7 |
| query | `query:execute / cancel` | 见 ipc-contract | ✅ 任务 7 |
| scripts | `script:open / save` | 见 ipc-contract | ✅ 任务 7 |
| export | `export:excel / export:insert` | 见 ipc-contract | ✅ 任务 7 |
| history | `history:list / add / remove` | 见 ipc-contract | ✅ 任务 7 |
| favorites | `favorites:list / save / remove / open` | 见 ipc-contract | ✅ 任务 6/7/11（**文件库**：落 `userData/queries/*.sql`，偏差决策 D1，不再走 SQLite） |
| ai | `ai.complete`（**V2 占位，V1 仅定义不注册**） | 契约已定义（任务 2），handler 待 V2 实装 | V2 |

> 注：任务 8 的 UI 组件通过 `window.sqlStudio['channel:name']()` 调用（channel 带冒号，用方括号索引）。`src/renderer/global.d.ts` 已声明全局类型。

---

## §7 坑位与经验记录（踩过即记，维护阶段必更新）

> 格式：`[日期] 问题 → 原因 → 解决办法`。按时间倒序。

- `[2026-08-20 会话5]` **monaco-editor 类型导入不稳定**
  → `import type { IMonarchLanguage } from 'monaco-editor'` 报 "no exported member"（0.56 类型入口走 typings 字段）；`monaco-editor/esm/vs/editor/editor.api` 在 bundler 解析下也可能找不到。
  → monaco-language.ts 改用**本地结构类型**（SqlStudioMonarchLanguage / SqlStudioThemeData），调用处 `as unknown as Parameters<...>[1]` 受控转换，测试无需真实 monaco。
- `[2026-08-20 会话5]` **monaco editor.addCommand 返回 string（命令 id），无 dispose**
  → 快捷键注册改用 `editor.addAction({ id, label, keybindings, run })`，返回 IDisposable 可清理。
- `[2026-08-20 会话5]` **@monaco-editor/react 默认从 CDN 加载 monaco**
  → 构建产物里没有 monaco（渲染 bundle ~478KB）。桌面离线包需 `import * as monaco from 'monaco-editor'; import { loader } from '@monaco-editor/react'; loader.config({ monaco })` 走本地 min 版（任务 13 处理）。
- `[2026-08-20 会话5]` **zustand store 是模块级单例，组件测试间状态泄漏**
  → app.test.tsx beforeEach 里 `useWorkspace.setState({ tabs: [], activeTabId: null, ... })` 重置，否则后置用例看到前置用例的标签。
- `[2026-08-20 会话5]` **getCurrentStatement 的 offset 越界返回空**
  → 光标 offset > sql.length（如点串尾之后）收敛到 sql.length 再处理。
- `[2026-08-20 会话5]` **splitStatements 保留语句前的注释前缀**
  → `'-- c;d\nSELECT 2'` 被当作一条语句（注释是前缀内容），与 MySQL CLI 行为一致；断句只认代码区 `;`。测试期望按此修正。
- `[2026-08-20 会话4]` **项目从 Windows 迁移到 NAS（linux x64）后原生模块与平台包缺失**
- `[2026-08-20 会话4]` **项目从 Windows 迁移到 NAS（linux x64）后原生模块与平台包缺失**
  → 7z 里的 node_modules 是 Windows 的：rollup linux 原生包缺失（`@rollup/rollup-linux-x64-gnu`）、better-sqlite3 的 `build/Release/*.node` 为 Windows PE（linux 加载报 `invalid ELF header`）。
  → 先 `npm install --ignore-scripts`（补齐平台可选包，脚本跳过防 electron 下载卡住），再 `npm rebuild better-sqlite3`（node-gyp 为当前 Node ABI 重编；NAS 有 g++/make/python3）。**重建后补齐 `tests/shared/ipc-contract.test.ts` 的 channel 列表并不能解决原生模块问题——两码事。**
- `[2026-08-20 会话4]` **`vite.config.ts`（渲染构建）缺 `@renderer/@preload/@main` 别名**
  → 上次会话只给 tsconfig/vitest.config 补了别名，`npm run build` 的渲染构建 `Rollup failed to resolve import "@renderer/components/..."`。
  → 三者同步：tsconfig.json paths ↔ vitest.config.ts alias ↔ vite.config.ts alias，缺一即 dev/test/build 三态不一致。已在 vite.config.ts 补齐。
- `[2026-08-20 会话4]` **jsdom 25 原生表单校验拦截 submit，组件测试点「保存」不触发 onSave**
  → `required` 字段为空时 jsdom 阻止 submit 事件（旧版 jsdom 直接放行）。
  → ConnectionForm 改用 `<form noValidate>` + 自定义 `validate()`（必填 name/host/user + 端口 1-65535），错误消息统一走 `form-error` 样式；测试需先填必填字段再点保存（符合真实用户流）。
- `[2026-08-20 会话4]` **测试文本匹配用精确匹配误判（findByText('id') 找不到）**
  → 目标元素完整文本含多个文本节点（如 `🔑 id bigint NOT NULL`），默认 `exact: true` 匹配整段失败。
  → 用 `findByText('id', { exact: false })` 或正则 `/users/`；同页多个匹配（如两张表各一个「预览」按钮）用 `findAllByText` + 下标。
- `[2026-08-20 会话4]` **NAS 无头环境跑不了 Electron GUI 冒烟**
  → `node_modules/electron` 内仍是 Windows 二进制，linux 无显示服务器也无法起窗口。
  → R3 冒烟降级为「App 级 jsdom 集成测试」`tests/renderer/app.test.tsx`（mock 全 channel：列表→选中→对象树→表→字段），GUI 冒烟待回 Windows 开发机执行 `npm run dev` 补验。
- `[2026-08-20]` **npm install 极慢 / Electron 二进制下不下来**
- `[2026-08-20]` **npm install 极慢 / Electron 二进制下不下来**
  → 无代理 + 官方源。
  → 项目根已写 `.npmrc`（代理 `127.0.0.1:7897` + npmmirror 镜像 + electron/better-sqlite3 二进制镜像）。
- `[2026-08-20]` **`node` 不是内部或外部命令**
  → Node 未装系统 PATH。
  → 用内置运行时安装器装了 Node 22.22.2（`C:\Users\LANGYAO\.workbuddy\binaries\node\versions\22.22.2`）并已加入用户 PATH；当前 shell 需手动 `$env:Path = "..."` 注入。
- `[2026-08-20]` **Electron 包装完没有 `node_modules/electron/dist`**
  → postinstall 未执行（下载失败被跳过）。
  → `cd node_modules/electron && node install.js` 手动补下（走 .npmrc 镜像）。
- `[2026-08-20]` **主进程构建输出 `.js` 时报 CJS 警告 / 运行时 require 报错**
  → `package.json` 是 `type: module`，Electron 需要 CJS。
  → 主进程/preload 产物固定 `.cjs`（vite.main.config.ts 已配 `entryFileNames: '[name].cjs'`）。
- `[2026-08-20]` **tsc 报 `Element implicitly has an 'any' type`（索引访问）**
  → 严格模式下字符串索引不自动推断。
  → 用 `as const` 数组 + 显式类型（见 `tests/main/scaffold.test.ts` 的 `scripts` 写法）。
- `[2026-08-20]` **better-sqlite3 装好后 main 进程 require 失败**
  → 原生模块 ABI 与 Electron 不一致（还没 rebuild）。
  → 任务 3 使用前必跑 `npm run rebuild`。
- `[2026-08-20]` **新增 IPC channel 后必须登记两处**：`IPC_CHANNELS` 常量 + `IpcRequestMap`/`IpcResponseMap` 映射，否则 `IpcRequest<C>`/`IpcData<C>` 取不到类型，且契约测试会报 channel 数量不一致。
- `[2026-08-20]` **渲染进程禁止持有 password**：对外连接对象统一用 `ConnectionSummary`（不含 password 字段），`ConnectionConfig`（含明文）仅主进程内部使用。
- `[2026-08-20]` **主进程测试导入用 `@main/*` 别名**：`tsconfig.json` 与 `vitest.config.ts` 均已加 `@main/*` → `src/main/*`（与 `@shared/*` 并列）。新写主进程服务单测用 `@main/services/xxx` 导入，保持统一。
- `[2026-08-20]` **better-sqlite3 需类型包**：`tsc` 报 `Could not find a declaration file`，已 `npm i -D @types/better-sqlite3`。普通 Node 跑主进程单测（vitest node 环境）可直接用 better-sqlite3（其预编译二进制对应 Node ABI）；`npm run rebuild` 仅为 Electron 运行时重编。
- `[2026-08-20]` **密码加密设计落地**：`Security` 优先用 Electron `safeStorage`（真加密），无 Electron 时降级 `b64:` base64 混淆并标记；`metadata-store` 的 `connections.password` 列只存密文，对外 `listConnections/getConnection` 返回 `ConnectionSummary`（无密码），仅 `getConnectionConfig` 返回含明文的主进程内部配置。
- `[2026-08-20]` **旧项目 web_sql_pro 迁移要点（已用 subagent 对齐）**：历史字段 time/sql/elapsed（多连接加 connectionId/connectionName）；旧项目收藏塞进 `saved_queries_pro.json`（id/name/sql/note/time），**用户明确不满其"只在网页面板看、不清晰、不便检索复用"**；**新决策 D1（偏差）**：命名收藏改为 `userData/queries/*.sql` 文件库（文件名即收藏名、顶部 `--` 注释块存元信息），历史仍留 SQLite。旧项目密码明文、无多连接、不支持多结果集——不照搬，按新架构重设计；Excel 表头深蓝 `#4472C4`+白字+冻结首行 A2（任务 6 对齐）。
- `[待补充]`

---

## §8 会话进度备注（每次会话结束时维护）

> 用途：下个话题 AI 先看这里，快速确认"上次干到哪、还有什么尾巴"。

- `[2026-08-20 会话1]` 任务 1 完成（骨架+冒烟全通过）。沉淀了「架构文档.md」（含状态表）与「开发文档.md」。**下一步：任务 2 main-shared-types**（定义 src/shared/types.ts 全量类型 + ipc-contract.ts 全量 channel，含 AI 占位）。
- `[2026-08-20 会话2]` 任务 2 完成（共享类型 + IPC 契约）。新增测试 `tests/shared/ipc-contract.test.ts`（10 例，类型编译+运行断言）。**下一步：任务 3 main-security-store**（security.ts + metadata-store.ts + 单测）。
- `[2026-08-20 会话3]` 任务 3 完成（安全层 + 元数据存储）。新增 `src/main/services/security.ts`、`metadata-store.ts`；测试 `tests/main/security.test.ts`（7 例）、`metadata-store.test.ts`（14 例）。用 code-explorer 子代理对齐旧项目 web_sql_pro 数据结构。**下一步：任务 4 main-connection**（connection-manager.ts + 单测）。
- `[2026-08-20 会话4]` 任务 8 完成（ui-connection 半成品收尾）。在 NAS(linux x64) 上解压 7z 后完成：修 ObjectExplorer 10 个 TS 错误（string[]→DbNode[]）、ConnectionForm 补表单校验、App.tsx 集成两栏工作台、theme.css 补组件/布局样式、vite.config.ts 补 @renderer 别名、ipc-contract 测试补 favorites:open。新增 object-explorer(7)/app(1) 测试。全量 tsc 零错误 + vitest 122/122 + build 通过，另修好 Windows→linux 原生模块（npm rebuild better-sqlite3）。**下一步：任务 9 ui-editor**（SqlEditor.tsx Monaco 封装 + EditorTabs.tsx；注意 GUI 冒烟需回 Windows 开发机）。
- `[2026-08-20 会话5]` 任务 9 完成（ui-editor）。新增 lib/sql-utils、lib/sql-completion（SchemaCompletionProvider）、lib/monaco-language（动态 tokenizer+主题）、store/workspace（zustand 多标签）、SqlEditor.tsx、EditorTabs.tsx；App 集成编辑器工作台 + 双击表生成 SELECT + 执行落位 workspace.execution。测试新增 45 例（纯逻辑 29 + 组件 16），全量 164/164 + tsc 零错 + build 通过。**下一步：任务 10 ui-results**（ResultTabs/ResultGrid 渲染 workspace.execution；虚拟滚动/排序/筛选/复制）。

---

## §9 AI 扩展指南（V2 接入点，本次仅预留不实装）

> 目标：V2 在 Monaco 中接入 OpenAI 兼容 API（DeepSeek/腾讯混元）实现 AI 行内补全建议。
> V1（本次）只做可插拔接口与占位，接入步骤留档如下。

### 9.1 已预留的接口与定义（随任务 2/7/9 落位）

- `src/shared/types.ts`：`CompletionProvider`（可插拔补全接口，V1 实现 `SchemaCompletionProvider` 规则补全）、`AiConfig`（model/baseUrl/apiKey，V2 用）、`AiCompletionRequest/Response`。
- `src/shared/ipc-contract.ts`：`ai.complete` channel 占位定义（V1 不注册 handler）。
- `src/main/services/metadata-store.ts`：settings 表预留（V2 存 AiConfig，apiKey 经 safeStorage 加密）。
- 渲染进程：`AiProvider` 类型定义。

### 9.2 V2 接入步骤（届时执行）

1. metadata-store 增加 `settings.get/set`（apiKey 加密存储）。
2. 主进程新增 `ai-service.ts`（fetch OpenAI 兼容接口，错误规范化），在 ipc.ts 注册 `ai.complete`。
3. 渲染进程实现 `AiCompletionProvider`，与 `SchemaCompletionProvider` 并列注册/可切换。
4. 编辑器中灰色行内预测 UI（Monaco `registerInlineCompletionsProvider`）。
5. 更新「开发文档.md」§7 坑位与本文档（删除"仅预留"标记）。

---

## §10 常用排障

| 症状 | 排查/处理 |
|---|---|
| `dev` 起不来，Electron 白屏 | 先确认 `dist/main/main.cjs` 存在（dev 脚本会先 build:main）；看 `dev-smoke.log` 或终端输出 |
| Vite 端口占用 | 改 `vite.config.ts` server.port，并同步改 package.json 的 `dev:electron`/`dev:renderer` 里 5173 |
| 组件测试跑在 node 环境报 DOM API 错误 | 文件头加 `// @vitest-environment jsdom` |
| 原生模块报 ABI mismatch | 跑 `npm run rebuild`（electron-rebuild 重编 better-sqlite3） |
| 主进程 require 不到 npm 包 | 该包被 external 了，运行时从 node_modules 加载 → 确认 `package.json` dependencies 里有它（不是 devDependencies） |

---

## §11 会话修复记录（2026-08-20，按偏差决策 D1 收口）

**背景**：任务 3（agent 已完成）的 `metadata-store.ts` 仍按旧设计实现了 `favorites` 表（建表 + `saveFavorite`/`listFavorites`/`removeFavorite`），与已确认的 D1（命名收藏改为 `queries/` 文件库）冲突；且 `types.ts`/`ipc-contract.ts` 已改为文件型 `FavoriteItem`（无 `id`），导致 `metadata-store.ts` 引用已删除字段、编译失败。

**本次修复**：
1. `metadata-store.ts`：删除 `favorites` 表建表 SQL 与三个 favorites 方法、移除 `FavoriteItem` 导入；现仅管 `connections` / `history` 两表。
2. 新增 `src/main/services/favorites-store.ts`：文件库实现，覆盖 `listFavorites`/`saveFavorite`/`removeFavorite`/`readFavorite`；文件名即收藏名、顶部 `--` 注释块存 name/connection/tags/createdAt、正文纯 SQL；含文件名安全化与重名加序号。
3. 新增 `tests/main/favorites-store.test.ts`（9 用例），并将 `metadata-store.test.ts` 旧收藏用例替换为「断言不再管 favorites」。
4. 运行 `vitest` 全绿（28 passed），`tsc --noEmit` 零错误。

**当前状态**：收藏文件化逻辑闭环完成，待任务 6/7/11 接入 `favorites-store`（IPC 注册 + UI 面板）即可端到端打通。

**任务 4 main-connection 完成（2026-08-20）**：新增 `src/main/services/connection-manager.ts` + `tests/main/connection-manager.test.ts`（18 passed）。要点：
- `ConnectionManager` 按连接 id 创建/复用 mysql2 池；`getPool` 复用、空闲超时自动 `end()` 并移出（计时器 `unref` 不阻塞退出，访问刷新）。
- `testConnection` 用临时单连接 + `SELECT 1` 探活，成功 `{ok:true}`，失败经 `normalizeConnectionError` 映射友好中文（ETIMEDOUT/ECONNREFUSED/ER_ACCESS_DENIED_ERROR 等分支）。
- `execute` 取池连接执行并 `release`；密码仅主进程持有（接收已解密 `ConnectionConfig`）。
- 依赖注入 `Mysql2Factory`，单测 mock 假 pool/connection；`POOL_CONFIG` 常量集中（MAX_CONNECTIONS=10 / IDLE_TIMEOUT=5min / CONNECT_TIMEOUT=10s / CHARSET=utf8mb4）。
- `tsc --noEmit` 零错误。`connections:test` 等 IPC 由任务 7 注册时引用本模块。

---

## §12 本次会话交接（2026-08-20，任务 5-7 完成 + 任务 8 半成品）

**给下一个 agent 的速读**：任务 1-7 已全部 completed；当前卡在**任务 8（半成品）**，详见下。做完任务 8 才能进 9/10/11/12/13。

### 本会话新增/变更文件清单
- `src/main/services/query-service.ts` + `schema-cache.ts`（任务 5）+ 测试 2 个（22 passed）
- `src/main/services/script-store.ts` + `excel-exporter.ts` + `sql-exporter.ts`（任务 6）+ 测试 3 个（28 passed，含 favorites-store）
- `src/main/ipc.ts`（任务 7）+ `src/preload/index.ts`（解包包装，`window.sqlStudio`）+ `src/renderer/global.d.ts` + 测试 1 个（7 passed）
- `src/renderer/components/ConnectionForm.tsx`、`ConnectionManager.tsx`、`ObjectExplorer.tsx`（任务 8，**未收尾**）+ `tests/renderer/connection-manager.test.tsx`（未跑）
- `tsconfig.json` / `vitest.config.ts`：补 `@renderer/*`、`@preload/*` 别名
- `src/shared/ipc-contract.ts`：`IPC_CHANNELS` 补 `favorites:open`；`types.ts`：收藏文件型（D1）

### 关键设计决策（新 agent 必读）
1. **IPC 包装/解包约定**：主进程 `ipc.ts` 统一 `{ok:true,data}` / `{ok:false,error}`；preload `invokeUnwrapped` 解包为「裸 data resolve / 失败 reject」。渲染进程直接用 `await window.sqlStudio['xxx']()`，异常走 try/catch。
2. **channel 带冒号**（如 `'app:ping'`），渲染进程用 `['app:ping']()` 方括号调用；App.tsx 已按此改。
3. **QueryService/ConnectionManager**：query 走 `ConnectionManager.executeMany`（临时多语句连接），schema 走 `SchemaCache`（executor 闭包注入）。
4. **收藏文件库 D1**：`FavoritesStore` 管理 `userData/queries/*.sql`，favorites:* IPC 已注册。

### 任务 8 遗留（必须先修，完成门槛 = tsc 零错误 + 组件测试通过）
1. `ObjectExplorer.tsx` 有 **11 个 TS 错误**：状态 `string[]` 被当 `{name,tables}` 对象用；`TableMeta` 无 `columns`。→ 把 `databases` 状态改为 `{ name: string; tables?: TableMeta[] }[]`，表节点扩展 `columns?: ColumnMeta[]`。
2. 修好后跑 `vitest run tests/renderer/connection-manager.test.tsx`（6 用例）。
3. 组件 CSS 类尚未写（`connection-form`/`conn-manager`/`explorer` 等），对齐 §7 深色 UI 规范。

### 环境提示
- Node 不在 PATH：用 `C:\Users\LANGYAO\.workbuddy\binaries\node\versions\22.22.2\node.exe` 直接跑 `node ./node_modules/vitest/vitest.mjs run --watch=false <file>` 和 `node ./node_modules/typescript/bin/tsc --noEmit`。
- 组件测试文件头部需 `// @vitest-environment jsdom`。

---

## §13 本次会话交接（2026-08-20 会话4，任务 8 完成，在 NAS linux x64 上执行）

**给下一个 agent 的速读**：任务 1-8 已全部 completed；**下一个任务是任务 9 ui-editor**（SqlEditor.tsx Monaco 封装 + EditorTabs.tsx）。以下为本会话要点，避免重踩。

### 本会话环境（与开发文档 §1 的 Windows 环境不同）
- **运行平台**：NAS linux x64（非原 Windows 开发机）；Node v22.23.2（原机 22.22.2）。
- node_modules 从 Windows 7z 带入 → 已 `npm install --ignore-scripts` 补齐平台包 + `npm rebuild better-sqlite3` 重编原生模块（NAS 有 g++/make/python3）。
- **Electron GUI 冒烟不可行**（node_modules/electron 仍是 Windows 二进制 + 无显示服务器）→ 任务 8 冒烟以 `tests/renderer/app.test.tsx` 集成测试替代；**任务 9/10 的 GUI 冒烟需回 Windows 开发机 `npm run dev` 补验**。

### 本会话变更文件清单
- `src/renderer/components/ObjectExplorer.tsx`：修 10 个 TS 错误（`string[]`→`DbNode[]`；`TableNode = TableMeta & { columns? }`）；加 `onOpenTable` 双击回调、loading/错误态、`db.table` 复合展开键。
- `src/renderer/components/ConnectionForm.tsx`：`noValidate` + 自定义校验（必填+端口 1-65535），`form-error` 样式。
- `src/renderer/App.tsx`：两栏工作台（ConnectionManager + ObjectExplorer + 占位区）。
- `src/renderer/styles/theme.css`：追加任务 8 组件样式 + 工作台布局。
- `vite.config.ts`：补 `@renderer/@preload/@main` 别名（渲染构建必需）。
- `tests/shared/ipc-contract.test.ts`：补 `favorites:open` channel（上次会话漏同步）。
- 新增测试：`tests/renderer/object-explorer.test.tsx`（7）、`tests/renderer/app.test.tsx`（1）；修 `connection-manager.test.tsx`（补必填字段）。
- `docs/架构文档.md` §6.1/§6.3-8、`docs/开发文档.md` §7/§8：同步任务 8 完成记录。

### 校验结果（R3 三门槛）
- `tsc --noEmit`：零错误。
- `vitest run`：**122/122 全过**（15 测试文件：main 9 + renderer 4 + shared 2）。
- `npm run build`：通过（dist/main/main.cjs + preload.cjs + dist/renderer/）。

### 给任务 9 ui-editor 的提示
- 起始状态：App.tsx 中央是 `workspace-placeholder` 占位区，任务 9 替换为 Monaco 编辑器 + EditorTabs。
- 编辑器实现参考 §6.3-9：可直接上 `@monaco-editor/react`；语义高亮/补全需要 schema 数据（`schema:*` IPC 已在任务 5/7 就绪，`SchemaCache` 在主进程）。
- 冒烟注意：Monaco 在 jsdom 测试需要 mock（monaco editor 无法在 jsdom 完整加载），参考 `@monaco-editor/react` 官方测试方案或抽离纯逻辑（补全项生成/tokenizer 规则）单测。
