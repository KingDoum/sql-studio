# SQL Studio 待办与遗留问题

> 用途：**唯一待办清单**。所有未完成事项集中在本文件，其余文档只描述「已实现的事实」。
> 更新时间：2026-09-16（第二轮清理：P1 全部完成 + P2 15 项完成）
> 核实原则：源码为准。已完成项见 §3 / §4 修复记录，不应再作为待办。

---

## 1. P0：发布阻断项（Windows 真机验收）

需要人工在 Windows 图形环境逐项确认（无法由自动化代劳）。

| # | 事项 | 状态 |
|---|---|---|
| 1 | 完整旅程冒烟（按 `e2e-checklist.md`：连接/查询/取消/导出/历史/收藏/文件对话框） | 🔄 进行中（启动、工作区恢复、连接列表、编辑器已验） |
| 2 | 文件对话框与路径边界（取消/覆盖/无权限/占用/非 ASCII/UNC） | ⏳ 待执行 |
| 3 | 三主题图形化走查（深色/白天已验证，钛灰待走查） | 🔄 部分 |
| 4 | AI 补全真机验收（连续多次触发、Tab 接受、suffix 影响） | 🔄 缺陷已修，待复验 |
| 5 | 自动保存与恢复真机验收（叉窗/kill/崩溃/升级安装） | 🔄 部分（标签恢复已验） |
| 6 | 崩溃路径日志样本（`child-process-gone` 等） | ⏳ 待执行 |
| 7 | ~~Windows 发布包重建与启动~~ | ✅ 已完成（本地构建 + 启动冒烟通过） |
| 8 | 回退演练（v2 库降级回旧版本） | ⏳ 待执行 |
| 9 | 备份与恢复策略（`sql-studio.db` / `-wal` / `-shm`） | ⏳ 未设计 |

> 本地打包已不依赖 GitHub Actions（见 `开发文档.md` §10）。正式 NSIS 安装包需开启 Windows 开发者模式或以管理员运行 `npm run package`。

---

## 2. 剩余待办（结构 / 环境 / 决策类）

| # | 事项 | 类型 | 说明 |
|---|---|---|---|
| 1 | 拆分 `App.tsx` 工作流 | 结构重构 | 当前约 740 行。**建议与功能改动分开的单独一轮进行**：需要提取多个自定义 hook 并重组状态，属于「改不行为」的重构，混在大批量功能修复里会显著降低可审查性 |
| 2 | 其余 IPC 的运行时输入校验 | 结构 | 已完成：`settings:set` 白名单、`script:*` 路径绝对性校验、`dataPreview` limit 钳制、`workspace:*` 入参校验；其余（`connections:save`、`history:add` 等）仍依赖编译期类型 |
| 3 | 真实 Electron smoke + 真实 MySQL 联调 | 环境 | 本机无 MySQL（3306/3307 不可达）、无 Docker；Playwright 无头 E2E 不能替代 |
| 4 | 普通查询统一走连接池 | 设计决策 | 现为临时多语句连接（`multipleStatements: true`），**有意保留**；改池需先解决多语句/安全选项/取消的兼容性 |
| 5 | 评估更稳定的数据库原生取消方案 | 评估 | 现为 abort 时硬销毁 stream + 本地 reject（MariaDB 实测约 400ms 生效） |

---

## 3. 2026-09-16 修复记录 · 第一轮（代码审查 P0/P1）

| 级别 | 问题 | 修复 |
|---|---|---|
| **P0** | 连接编辑表单无 `key`，切换编辑目标时把 A 的配置覆盖到 B | `ConnectionManager.tsx` 加 `key={editing?.id ?? 'new'}` |
| P1 | Excel 导出用列名作列 key，JOIN 同名列后值覆盖前值 | `excel-exporter.ts` 列 key 改用下标 `col_${idx}` |
| P1 | 一条坏密文导致整个连接列表加载失败 | 摘要路径改走**不解密**的 `rowToSummary`；`rowToConfig` 失败隔离为空密码 |
| P1 | `b64:` 降级密文 / 历史明文在 safeStorage 环境下解不开 | `security.ts` 三种格式各自解码；`hex:` 无 safeStorage 时给可读错误 |
| P1 | 预载表请求读了序号却不比较 → 切连接脏写 | `ObjectExplorer.tsx` 引入**连接代际 `epochRef`** |
| P1 | `loadTables`/`loadColumns` 共用计数器互相作废 → 节点永久空白 | 改为只读代际 |
| P1 | 字段缓存键不含连接 → 切连接显示上个连接的字段 | `TableFieldsPanel.tsx` 连接变化清空缓存 + 作废在途请求 |
| P1 | 删除任意连接都清空当前选中 | 仅 `id === selectedId` 时清空 |
| P1 | AI 超时双源（Main 硬编码 15s vs 设置可调） | `ai-service.ts` 改用共享归一化的 `rateLimit.requestTimeoutMs` |
| P1 | `AiService` 日志用 `console.log`，打包后丢失 | 注入日志出口到持久日志（`event: ai.request`） |
| P1 | 白天模式下编辑器启动仍为深色 | `SqlEditor.tsx` 用 `themeRef` + `monaco.editor.setTheme()` |
| P1 | AI 补全「触发一次后再也不触发」 | `ai-provider.ts` 的 `disposeInlineCompletions` 改为 no-op（Monaco 结果级回收 ≠ provider 销毁） |
| P1 | 日志 `flush()` 无意义表达式 | 清理，并更正注释（electron-log file transport **默认 `sync: true`**） |
| — | 导出/另存为默认文件名 | 新增 `lib/file-name.ts`，默认名带 `YYYYMMDD_HHmmss` |

---

## 4. 2026-09-16 修复记录 · 第二轮（P1 全清 + P2 15 项）

### 4.1 P1 功能缺口（全部完成）

| # | 事项 | 修复 |
|---|---|---|
| 1 | `schema:dataPreview` 未复用查询服务 | 改走 `makeExecutor → executeMany → QueryService` 同一链路，语句切分/写标记/耗时统计不再失真 |
| 2 | safeStorage 降级时 UI 无提示 | 新增 `app:securityStatus` channel；状态栏显示「密码为降级存储」警告（悬停说明 base64 非真加密） |
| 3 | 文件路径与 settings key 无运行时校验 | `settings:set` 白名单（7 个键）；`script:open/save/stat` 强制绝对路径 + 拒绝 NUL；移除「缺省文件名写到进程 cwd」的历史行为 |
| 4 | 外部文件冲突提示缺失 | 打开/保存记录并对比文件 mtime（`script:stat` + `EditorTab.mtimeMs`）；被外部改过时保存前 `confirm` 确认 |
| 5 | 结果区高度未持久化 | 拖拽结束写入 `settings.resultPanelHeight`，启动时恢复 |
| 6 | electron-log `eventLogger` 未启用 | **改为手动注册 `child-process-gone`**：eventLogger 会绕过本项目日志脱敏链路，手动注册可复用受控路径 + 脱敏 + 轮转 |

### 4.2 P2 结构/正确性/可靠性（15 项）

| # | 事项 | 修复 |
|---|---|---|
| 1 | 拆分 `ipc.ts`（433 行单文件） | 拆为 `ipc-deps.ts`（依赖/校验/工具）、`ipc-handlers-db.ts`（连接/Schema/查询/历史）、`ipc-handlers-io.ts`（脚本/导出/收藏/对话框）、`ipc-handlers-app.ts`（应用/AI/设置/工作区/日志）、`ipc.ts`（组装） |
| 2 | `main/index.ts` 注释「27 个 handler」过时 | 更正为 41（新增 2 个 channel 后为 43） |
| 3 | `sql-classify.ts` 注释引用已归档文档 | 指向 `docs/archive/后续AI执行指令.md` |
| 4 | `ResultGrid` 用列名作 React key（同名列重复） | 表头/筛选行 key 改用列下标 |
| 5 | 结果区筛选无防抖（5 万行逐键全量过滤） | 输入停止 250ms 后才应用（`debouncedFilters`） |
| 6 | 切换结果集未重置筛选/排序/列宽 | `ResultTabs` 给 `ResultGrid` 加 `key={executedAt-safeSet}` |
| 7 | 收藏重命名框 Escape 连带关闭弹窗 | `stopPropagation`，只取消重命名 |
| 8 | AI 设置在加载失败后仍可保存（用默认值覆盖真实配置） | 新增 `loadFailed` 状态，禁用保存并在提示中说明原因 |
| 9 | Monaco 主题键 `editorLineHighlightBackground` 拼写错误 | 更正为 `editor.lineHighlightBackground`（三套主题的当前行高亮恢复） |
| 10 | 补全图标 CompletionItemKind 映射错位 | 按枚举修正：keyword=17 / column=3 / ai=27 / database=5 / text=18 |
| 11 | `formatDateLocal` 把真实的 00:00:00 显示成纯日期 | 按**列类型**判断（DATE 才省略时间）；无列类型信息时保留旧启发式兼容 |
| 12 | 历史/收藏删除失败静默 | 改为 `alert` 提示失败原因 |
| 13 | `workspace-persistence` 重试预算不重置（一次失败后长期只试 1 次） | 每次 `drain()` 重置 `retryAttempt` |
| 14 | 持久日志 `dropped` 计数口径错误 | 批次字节超限时按 `acceptedInputs.length - i` 计入 |
| 15 | 持久日志 `read()` 全量读文件再解析 | 文件按 mtime 倒序 + 从尾部解析，凑够 limit 即停 |
| 16 | `query-service` Buffer 分支不可达 + `.buffer` 丢 byteOffset | 合并为单一 `Uint8Array` 分支并复制等长副本 |
| 17 | `connection-manager` 三元两分支相同 | 简化为单次调用 |
| 18 | `log-redaction.shortHash` 前缀 `sha256:` 名不副实 | 改为 `fnv1a:`（与算法一致） |
| 19 | `metadata-store.setAiConfig` 的 `Buffer.isBuffer` 死分支 | 移除（`Security.encrypt` 只返回 string） |
| 20 | `defaultModelFor('deepseek-fim')` 返回不存在的 `deepseek-v4-pro` | 改为官方 FIM 模型 **`deepseek-flash`**（已核对 DeepSeek 官方文档） |

### 4.3 验证

| 项 | 结果 |
|---|---|
| `npm run typecheck` | ✅ 通过 |
| `npm test` | ✅ **42 文件 / 552 用例通过 + 1 跳过**（本轮净增 18 条回归测试） |
| Lint | ✅ 0 问题 |
| 打包 + 启动冒烟 | ✅ 通过（`app.starting` → `db.ready schema_version=2`） |

---

## 5. 已核实「其实已完成」（原文档描述已过时，勿再当待办）

| 原文档描述 | 实际状态 | 证据 |
|---|---|---|
| `readFavorite()` 与删除/重命名的回扫逻辑不一致 | ✅ 已统一 | `favorites-store.ts` 统一走 `resolveFavoritePath` |
| 未完全转义表名/列名，属待修复风险 | ✅ 已修复 | `escapeIdent` / `resolveTableIdent` |
| 写操作识别「后续应覆盖 CTE/CALL」 | ✅ 已实现 | `shared/sql-classify.ts` Main/Renderer 共用 |
| `QueryResultSet.elapsedMs` 实际返回 0 | ✅ 已修复 | 改为累计耗时 |
| AbortSignal 未贯通 | ✅ 已贯通 | `makeExecutor` → `executeMany(signal)` → 硬销毁 |
| better-sqlite3 ABI 问题 | ✅ 已解决 | Node 24 + 12.11.1；打包用 electron-v136 预编译包 |
| 退出时 `closeAll()` 时序 | ✅ 已修复 | `will-quit` + `preventDefault` + await（含 3s 超时） |
| 数据预览「待实现」/ Ctrl+S「待实现」 | ✅ 已实现 | `DataPreviewModal` / `SqlEditor` Ctrl+S 回调 |

---

## 6. 已实现未验证（受环境限制，非代码缺失）

1. 真实 DeepSeek API 调用（需有效 Key 与网络）——FIM 请求/响应已按官方样例单测；默认模型名已按官方文档校正。
2. Windows Electron 图形验收（§1 的大部分条目）。
3. 真实 MySQL/MariaDB 联调（本机无数据库实例）。
4. 打包产物在目标机器上的安装态验证（当前以免安装版直接运行验证）。

---

## 7. 文档归档说明（2026-09-16）

| 归档文档 | 归档原因 |
|---|---|
| `后续AI执行指令.md` | 一次性执行指令，S0-S7 已执行完毕 |
| `升级优化实施计划.md` | 执行计划主文档，S0-S7 已完成 |
| `UI重设计实施规范.md` | UI 重设计 S1-S5 + 独立审查已完成 |
| `SQL Studio 自动保存、工作区恢复与调试日志持久化实施方案.md` | 方案已实施完成（S0-S6） |
| `SQL Studio 自动保存与持久日志最终验收报告.md` | 历史验收报告，残余风险已合并至本文件 |

`docs/` 根目录仅保留长期有效的基线文档：`架构文档.md`、`开发文档.md`、`测试与发布验收规范.md`、`e2e-checklist.md`、本文件。
